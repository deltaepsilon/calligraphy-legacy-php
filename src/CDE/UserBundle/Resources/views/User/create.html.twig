{% extends '::admin.html.twig' %}

{% block bottomNav %}
    <a href="{{ path('CDEUserBundle_index') }}">
        <li>
            Back to Users
        </li>
    </a>
{% endblock %}

{% block main %}
    <h1>Create New User</h1>
    <form action="{{ path('CDEUserBundle_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
     <input type="submit" value="Create" />
     </form>
{% endblock %}
